# QML Guidelines

## Components

Keep components small and reusable.

## Naming

Use descriptive PascalCase component names.

## State

Keep local transient state local. Shared system state belongs to services.

## Styling

Use Theme tokens instead of arbitrary colors, spacing and radius values.

## Signals

Prefer clear signals and small APIs. Avoid tightly coupling unrelated modules.
