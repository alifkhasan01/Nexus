# Non-Goals

The project will not:

- implement a Wayland compositor
- implement a window manager
- implement a display server
- replace systemd
- replace PipeWire
- replace NetworkManager
- replace BlueZ
- implement authentication or PAM
- become a Linux distribution
- require one specific compositor when an abstraction is practical

The shell integrates with existing infrastructure rather than replacing it.
