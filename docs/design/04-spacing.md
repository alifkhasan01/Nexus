# Spacing

Use a consistent spacing scale.

Example:

```text
xs   = 4
sm   = 8
md   = 12
lg   = 16
xl   = 24
xxl  = 32
```

The exact values can evolve, but components should consume tokens instead of inventing new values.
